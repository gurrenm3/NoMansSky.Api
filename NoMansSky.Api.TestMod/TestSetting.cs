using Reloaded.ModHelper;

namespace NoMansSky.Api.TestMod;

internal class TestSetting : ModPropertyAttribute
{
    public override void OnAttributeLoaded()
    {
        /*ConsoleUtils.WriteLine("TestSetting Start");
        if (ClassInstance != null)
            Info.TargetProperty.SetValue(ClassInstance, 696969);
        else
            ConsoleUtils.WriteLine("NUL!");

        ConsoleUtils.WriteLine("TestSetting End");*/
    }
}
